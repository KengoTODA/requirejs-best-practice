{
  "name": "requirejs-best-practice",
  "version": "0.1.0",
  "scripts": {
    "test": "for project in `find $(pwd)/src -type d -maxdepth 1 -mindepth 1`; do cd $project && npm install && grunt --verbose; done"
  },
  "devDependencies": {
    "grunt-cli": "0.1.6"
  }
}
